spring:
  application:
    name: demo

management:
  endpoint:
    health:
      probes:
        add-additional-paths: true
        enabled: true
  endpoints:
    web:
      base-path: /
      exposure:
        include:
          - health
  server:
    port: 8081
